apiVersion: v1
kind: Secret
metadata:
  name: { { .Chart.name } }-secrets
  namespace: { { .Values.common.namespace } }
type: Opaque
data:
  { { - range $k, $v:= .Values.secrets } }
    { { $k } }: { { $v } }
    { { - end } }