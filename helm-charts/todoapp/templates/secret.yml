apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: {{ .Values.common.namespace }}
type: Opaque
data:
  { { - range $k, $v:= .Values.secrets } }
    { { $k } }: { { $v } }
    { { - end } }